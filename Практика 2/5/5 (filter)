a = input()
b = a.split()

#d - максимальная длина элемемента в списке b
d = max(b, key=len)

#фильтрация результатов в с
c = list(filter(lambda x: len(str(x)) == len(d), b))

#вывод отсортированного с и числа его элементов
print(sorted(c))